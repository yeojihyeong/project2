<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.micol.prj.follow.service.FollowMapper">

	<resultMap type="co.micol.prj.following.service.MemberVO"
		id="MemberVO">
		<result property="member_id" column="member_id" />
		<result property="member_picture" column="member_picture" />
		<result property="member_pfile" column="member_pfile" />

		<association property="followingVO"
			javaType="co.micol.prj.following.service.FollowingVO">
			<id property="followee" column="fosllowee" />
			<result property="follow_id" column="follow_id" />
			<result property="follower" column="follower" />
		</association>
	</resultMap>


	<!-- <select id="followSelectList"
		parameterType="co.micol.prj.following.service.MemberVO"
		resultMap="memberVO">
		SELECT M.MEMBER_PICTURE, M.MEMBER_PFILE, F.FOLLOWER FROM
		MEMBER M LEFT JOIN FOLLOWING F ON M.MEMBER_ID = F.FOLLOWEE WHERE
		FOLLOWEE=#{followee}
	</select>

	<select id="followeeSelectList"
		parameterType="co.micol.prj.following.service.MemberVO"
		resultMap="MemberVO">
		SELECT M.MEMBER_PICTURE, M.MEMBER_PFILE, F.FOLLOWER FROM MEMBER M LEFT JOIN
		FOLLOWING F ON M.MEMBER_ID = F.FOLLOWEE WHERE FOLLOWER=#{follower}
	</select> -->

</mapper>